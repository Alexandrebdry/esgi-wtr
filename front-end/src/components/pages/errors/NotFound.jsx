import {Box, Typography} from "@mui/material";

export default function ({}) {
    return (
        <Box height={'100vh'} flexDirection={"column"} display={"flex"} alignItems={"center"} justifyContent={"center"}>
            <Typography variant={"h1"} component={"h1"}>La page n'existe pas...</Typography>
            <Typography variant={"h2"}>une autre fois peut-être :D</Typography>
        </Box>
    )
}